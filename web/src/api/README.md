# Generated API client

`schema.ts` is generated from `api/openapi/code-winch.yaml`. Do not edit it by
hand and do not add a separately maintained API contract here.

Run `npm run api:generate` after changing the OpenAPI source. CI runs
`npm run api:check`, which regenerates the file and fails when the checked-in
output has drifted.
